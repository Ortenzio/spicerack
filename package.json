{
  "name": "spicerack",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @ozio/spicerack",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.1"
  }
}
