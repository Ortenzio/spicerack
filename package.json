{
  "name": "spicerack",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @ozio/spicerack",
    "build": "npm run build --workspaces --if-present"
  }
}
